import numpy as np

def f(x):
    """
    Заданная функция для интегрирования.
    f(x) = 1 / (x^2 * (x+1)^2)
    """
    if np.any(x == 0) or np.any(x == -1):
        pass
    return 1 / (x**2 * (x + 1)**2)

# Точное значение интеграла для сравнения
# I = 21/20 + 2 * ln(5/8)
I_exact = 1.05 + 2 * np.log(5/8)